{
  "logLevel": "debug",
  "database": {
    "schema": "isp_system_service",
    "database": "{{ msp_pgsql_name }}",
    "password": "{{ msp_pgsql_password }}",
    "username": "{{ msp_pgsql_user }}",
    "host": "{{ msp_pgsql_address }}",
    "port": {{ msp_pgsql_port }}
  },
  "redis": {
    "address": {
      "ip": "{{ msp_redis_address }}",
      "port": "{{ msp_redis_port }}"
    },
    "defaultDB": 0,
    "password": "{{ msp_redis_password }}"
  },
  "applicationSecret": "{{ msp_app_secret }}"
}
